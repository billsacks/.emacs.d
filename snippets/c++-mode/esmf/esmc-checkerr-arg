# -*- mode: c++ -*-
# name: Add logic to check the localrc from an ESMF call; if error, result is set in the rc pointer and we return
# key: esmc-checkerr-arg
# --
if (ESMC_LogDefault.MsgFoundError(localrc, ESMCI_ERR_PASSTHRU, ESMC_CONTEXT, rc))
  return$0;